spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true # 실행 되는 SQL문을 보여 줄지 설정
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

    database: mysql

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tutorial?useUnicode=true&characterEncoding=utf8&autoReconnect=true&useSSL=false&rewriteBatchedStatements=true&profileSQL=true&logger=Slf4JLogger
#    url: jdbc:mysql://localhost:3306/tutorial?useUnicode=true&characterEncoding=utf8&autoReconnect=true&useSSL=false
    username: root
    password: passw0rd